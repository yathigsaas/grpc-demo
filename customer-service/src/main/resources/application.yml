server:
  port: 8083

spring:
  application:
    name: customer-service

grpc:
  client:
    user-service:
      address: 'static://localhost:9091'
      negotiation-type: plaintext
      enable-keep-alive: true